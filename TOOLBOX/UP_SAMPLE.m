	function y = UP_SAMPLE(x, L)		% Inserts L-1 zeros between each sample in vector x 	%	Toolbox for DIGITAL FILTERS USING MATLAB		% 	Author: 		Lars Wanhammar 2003-06-24	% 	Modified by: 	LW 2015-02-02	% 	Copyright:		by authors - not released for commercial use	%	Version: 		1 	%	Known bugs: 	% 	Report bugs to:	Wanhammar@gmail.se	%			k = length(x);	y = 0;	if L > 1		for j = 1:k-1			y((j-1)*L+1) = x(j);			y((j-1)*L+2:j*L) = zeros(size(1:L-1));		end		y((k-1)*L+1) = x(k);	else		y = x;	end